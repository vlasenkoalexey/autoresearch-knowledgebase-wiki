---
slug: rlm
repo: raw/code/rlm
acquire: submodule
docs: [README.md]
synthesis_focus: recursive language models — how the REPL environment offloads a long prompt into a variable the root model queries via code, how sub-RLM calls are dispatched and their results bounded back into the parent's context, and the training/inference-engine split
---

## Concepts
